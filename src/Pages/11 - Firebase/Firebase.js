import React from 'react';
import { Frame, Container, Header2 } from '../../App/Components';

function Firebase() {  
  return (
    <Frame>
      <Container id="Firebase">
        <Header2 innerValue="React With Firebase" />
        <hr />
        
      </Container>
    </Frame>
  )
}

export default Firebase